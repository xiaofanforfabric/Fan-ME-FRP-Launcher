# Fan-ME-FRP-Launcher 更新日志 JSON 格式说明

## API 地址

```
https://api.frpc.xiaofanshop.cn/tpca.json
```

## JSON 格式

网站会从上述地址拉取 JSON 数据，并在文档页面的「更新记录」区域展示。

### 基本结构

```json
{
  "data": {
    "版本号": {
      "date": "发布日期",
      "note": "版本备注（可选）",
      "changes": [
        "更新内容1",
        "更新内容2",
        "更新内容3"
      ]
    }
  }
}
```

### 完整示例

```json
{
  "data": {
    "1.0.0": {
      "date": "2025-05-18",
      "note": "首个正式版本",
      "changes": [
        "支持 GUI 图形界面模式",
        "支持命令行模式启动",
        "自动检测系统架构并下载对应平台 frpc",
        "支持快捷启动模式（通过隧道 ID 启动）",
        "内置 GUI API 服务"
      ]
    },
    "1.0.1": {
      "date": "2025-06-01",
      "note": "",
      "changes": [
        "修复了某个 Bug",
        "优化了启动速度"
      ]
    }
  }
}
```

### 简化格式（也支持）

如果不需要 `date` 和 `note` 字段，可以直接用数组：

```json
{
  "data": {
    "1.0.0": [
      "支持 GUI 图形界面模式",
      "支持命令行模式启动",
      "自动检测系统架构并下载对应平台 frpc"
    ],
    "1.0.1": [
      "修复了某个 Bug",
      "优化了启动速度"
    ]
  }
}
```

## 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `data` | Object | ✅ | 顶层容器，包含所有版本 |
| `版本号` (key) | String | ✅ | 版本号，如 `"1.0.0"`、`"1.1.0"`（不需要加 `v` 前缀，网站会自动加） |
| `date` | String | ❌ | 发布日期，格式 `YYYY-MM-DD`，如 `"2025-05-18"` |
| `note` | String | ❌ | 版本备注，如 `"首个正式版本"`、`"紧急修复"` 等 |
| `changes` | Array | ✅ | 更新内容列表，每项是一个字符串 |

## 注意事项

1. **版本号排序**：网站会自动按版本号从高到低排序，最新版本显示在最前面
2. **最新版本标记**：排序后的第一个版本会自动标记为「最新版本」
3. **分页显示**：默认每页显示 5 个版本，超过会自动分页
4. **错误处理**：如果 API 无法访问或返回格式错误，页面会显示「暂没更新日志」

## 如何部署

将上述格式的 JSON 文件部署到 `https://api.frpc.xiaofanshop.cn/tpca.json` 即可，网站会在用户访问文档页面时自动拉取。